import React from 'react';
import { Route } from 'react-router-dom';

interface PrivateRouteProps {}

const PrivateRoute: React.FC<PrivateRouteProps> = ({}: PrivateRouteProps) => {
  return <></>;
};

export default PrivateRoute;
